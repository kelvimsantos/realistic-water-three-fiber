import { useControls } from 'leva'

const Controls = () => {
  // Os controles já estão integrados no componente Water via useControls
  // Este componente é apenas para organizar a UI
  
  return null
}

export default Controls